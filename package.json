{
  "name": "biletflow",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@12.3.4",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "db:generate": "dotenv -e .env -- pnpm --filter @biletflow/db run generate",
    "db:migrate": "dotenv -e .env -- pnpm --filter @biletflow/db run migrate",
    "db:deploy": "dotenv -e .env -- pnpm --filter @biletflow/db run deploy",
    "db:studio": "dotenv -e .env -- pnpm --filter @biletflow/db run studio",
    "db:seed": "dotenv -e .env -- pnpm --filter @biletflow/db run seed",
    "db:reset": "dotenv -e .env -- pnpm --filter @biletflow/db run reset",
    "infra:up": "docker compose -f infra/docker-compose.yml up -d",
    "infra:down": "docker compose -f infra/docker-compose.yml down",
    "infra:logs": "docker compose -f infra/docker-compose.yml logs -f"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
